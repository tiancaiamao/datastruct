/*
作者:genius
说明:这个文件有结点的结构，和一些用于链表操作的函数
带头结点的单链表
*/
#ifndef _LIST
#define _LIST
#include "utils.h"

//list就定义为node* ，不封装任何东西 提供最大的灵活性        
typedef pnode list;

//以下是函数
//创建链表
int createlist(list* head);
//销毁链表
void destroylist(list* head);
//要给定结点的后面查找
pnode find(pnode p,Elemtype data);
#endif
